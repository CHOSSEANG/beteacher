spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    username: sseang
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  application:
    name: beteacher
